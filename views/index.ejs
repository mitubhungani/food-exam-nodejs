<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Foods</title>
</head>
<body>
    <div>
        <h1>Home</h1>
    </div>
    <div>
        <% food.forEach((f) => { %>
            <div>
                <img src="http://localhost:8090/<%= f.image ? f.image : 'default.jpg' %>" alt="User Image" />
                <h3><%= f.name %></h3>
                <p><%= f.description %></p>
                <p><%= f.price %></p>
            </div>
        <% }) %> 
    </div>
</body>

</html>
